model m of Graph
{
    a is Node(1).
    b is Node(2).
    c is Node(3).
    d is Node(4).
    e is Node(5).
    f is Node(6).
    g is Node(7).
    h is Node(8).

    Edge(a, b).
    Edge(a, c).
    Edge(a, e).
    Edge(a, g).
    Edge(b, c).
    Edge(c, d).
    Edge(d, e).
    Edge(e, f).
    Edge(f, g).
    Edge(g, h).
}